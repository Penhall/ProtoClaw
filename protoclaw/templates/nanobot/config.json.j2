{
  "agents": {
    "defaults": {
      "model": "{{ model | default('claude-opus-4-6') }}"
    }
  },
  "providers": {
    "anthropic": {
      "apiKey": "{{ anthropic_api_key | default('') }}"
    },
    "openai": {
      "apiKey": "{{ openai_api_key | default('') }}"
    },
    "gemini": {
      "apiKey": "{{ google_ai_api_key | default('') }}"
    }
  }
}
